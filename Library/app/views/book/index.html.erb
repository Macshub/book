<% content_for :title, (I18n.t "book.index.title") %>
<div class="row">
  <div class="small-12 columns">
    <h1><%= I18n.t "book.index.title" %></h1>
    <% if @books.empty? %>
      <p><%= I18n.t "book.index.books_empty" %></p>
    <% else %>
      <ul>
        <% @books.each do |book| %>
          <li><%= book.title %></li>
        <% end %>
      </ul>
    <% end %>
    <%= link_to (I18n.t "book.new.title"), book_new_url %>
  </div>
</div>
